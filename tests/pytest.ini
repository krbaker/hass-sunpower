[pytest]

makers =
  pvs: Tests that work directly against a pvs to verify its working correctly (not if PVS env == MOCK this will test the Mock)
  ess: Tests that work directly against an ESS to verify its working correctly (not if PVS env == MOCK this will test the Mock)
  hass: Tests that validate home assistant functionality

env =
  PVS: MOCK
